This repo works as an obsidian vault that builds a website using babashka automations together with the obsidian cli, using obsidian properties for metadata.

`bb build` writes the published site into `public/`. `bb dev` serves it at http://localhost:8080/catalyst/ with drafts included, rebuilds on save and reloads the browser.
